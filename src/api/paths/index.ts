import { appConfig } from "../../config";

export const loginPath = `${appConfig.apiBaseUrl}/auth/login`;
export const getProfilePath = `${appConfig.apiBaseUrl}/auth/profile`;
export const signUpPath = `${appConfig.apiBaseUrl}/users/signup`;
export const competitionsPath = `${appConfig.apiBaseUrl}/competitions`;
